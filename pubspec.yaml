name: dartralph
description: Minimal AFK harness — drains ready-for-agent GitHub issues per PRD via the claude CLI.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.6.0

executables:
  dartralph: dartralph

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
