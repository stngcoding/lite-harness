'use strict';

var chunkR6FB52AD_cjs = require('../chunk-R6FB52AD.cjs');

// src/adapters/gemini.ts
function withHeadroom(model, options = {}) {
  const originalGenerate = model.generateContent.bind(model);
  const originalStream = model.generateContentStream?.bind(model);
  const compressedGenerate = async (params) => {
    const contents = params.contents ?? params;
    const modelName = options.model ?? "gemini-2.0-flash";
    const result = await chunkR6FB52AD_cjs.compress(
      Array.isArray(contents) ? contents : [contents],
      { stack: "adapter_ts_gemini", ...options, model: modelName }
    );
    const newParams = Array.isArray(params) ? result.messages : { ...params, contents: result.messages };
    return originalGenerate(newParams);
  };
  const compressedStream = originalStream ? async (params) => {
    const contents = params.contents ?? params;
    const modelName = options.model ?? "gemini-2.0-flash";
    const result = await chunkR6FB52AD_cjs.compress(
      Array.isArray(contents) ? contents : [contents],
      { stack: "adapter_ts_gemini", ...options, model: modelName }
    );
    const newParams = Array.isArray(params) ? result.messages : { ...params, contents: result.messages };
    return originalStream(newParams);
  } : void 0;
  return new Proxy(model, {
    get(target, prop) {
      if (prop === "generateContent") return compressedGenerate;
      if (prop === "generateContentStream" && compressedStream)
        return compressedStream;
      return target[prop];
    }
  });
}

exports.withHeadroom = withHeadroom;
//# sourceMappingURL=gemini.cjs.map
//# sourceMappingURL=gemini.cjs.map